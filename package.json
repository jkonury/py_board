{
  "name": "py_board",
  "version": "1.0.0",
  "description": "pip install -U -r requirements.txt python manage.py --settings=py_board.settings.local python manage.py migrate --settings=py_board.settings.local python manage.py runserver 8080 --settings=py_board.settings.local",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
